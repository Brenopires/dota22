---
phase: 02-hero-identity
plan: "06"
type: execute
wave: 4
depends_on: ["02-04", "02-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "At least 6 heroes are selectable with distinct combat roles"
    - "Every hero has a functional R-slot ultimate with 60-120s cooldown"
    - "Every hero has a passive that visibly triggers with VFX"
    - "XP bar is visible in HUD and fills during a match"
    - "Heroes reach level 5+ in a 5-minute match"
    - "TypeScript compiles with zero errors"
  artifacts:
    - path: "src/types.ts"
      provides: "AbilityDef with R slot, PassiveDef interface"
      contains: "'Q' | 'W' | 'E' | 'R'"
    - path: "src/systems/XPSystem.ts"
      provides: "XP accumulation system"
      contains: "class XPSystem"
    - path: "src/heroes/heroData.ts"
      provides: "13 heroes with Q/W/E/R abilities and passive"
      contains: "slot: 'R'"
    - path: "src/ui/HUD.ts"
      provides: "XP bar and level display"
      contains: "xpGraphics"
    - path: "src/ui/AbilityBar.ts"
      provides: "4-slot ability bar with gold R slot"
      contains: "isUltimate"
  key_links:
    - from: "src/systems/XPSystem.ts"
      to: "src/entities/Hero.ts"
      via: "killer.gainXP(XP_PER_KILL)"
      pattern: "gainXP"
    - from: "src/entities/Hero.ts"
      to: "src/systems/EventBus.ts"
      via: "passive subscription in constructor and cleanup in destroy()"
      pattern: "subscribePassive"
---

<objective>
Human verification of the complete Phase 2 feature set across all 5 success criteria. Claude runs a full match simulation (best effort in the browser environment), then the user plays and confirms the observable behaviors.

Purpose: Plans 02-01 through 02-05 implemented mechanics — this plan confirms they work together as a system during real gameplay.
Output: Approved Phase 2 or specific issues identified for gap closure.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final TypeScript and build validation</name>
  <files></files>
  <action>
Run the following checks and fix any issues found:

1. `npx tsc --noEmit` — must exit with code 0, zero errors. If errors remain, fix them before continuing.

2. Verify artifact count:
   - `grep -c "slot: 'R'" src/heroes/heroData.ts` — must output 13
   - `grep -c "passive:" src/heroes/heroData.ts` — must output 13
   - `grep "abilityCooldowns: number\[\] = \[0, 0, 0, 0\]" src/entities/Hero.ts` — must match
   - `grep "gainXP\|levelUp" src/entities/Hero.ts` — both must be present
   - `grep "xpGraphics" src/ui/HUD.ts` — must be present
   - `grep "isUltimate\|0xFFD700" src/ui/AbilityBar.ts` — both must be present

3. Start the dev server (`npm run dev`) — confirm no console errors on initial load.

4. If any check fails: fix the issue in the relevant file, re-run `npx tsc --noEmit`, confirm green before proceeding to the checkpoint.
  </action>
  <verify>
All grep checks pass. `npx tsc --noEmit` exits 0. `npm run dev` starts without console errors.
  </verify>
  <done>TypeScript compiles clean. All 13 heroes have R slots and passives. XP bar and 4-slot AbilityBar present. Dev server starts without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 Hero Identity system:
- 13 heroes each with Q/W/E/R abilities + passive
- R-slot ultimates (60-120s cooldown) bound to R key
- EventBus combat events: HERO_HIT, DAMAGE_TAKEN, HERO_KILLED
- Passive abilities that visibly trigger (gold VFX) on conditions
- XP system: 50 XP per kill, level thresholds, stat scaling
- Level-up VFX (gold burst + floating "LEVEL N!" text)
- HUD XP bar with level indicator
- 4-slot AbilityBar with gold R slot
  </what-built>
  <how-to-verify>
Open the game at localhost:8080 (or wherever `npm run dev` serves it) and play a full match. Verify each of the 5 success criteria:

**Criterion 1 — Hero variety:**
- The match starts with heroes on screen. Note that at least 6 distinct archetypes exist (Tank, Assassin, Mage, Carry, Support). The 13 heroes cover all these roles.

**Criterion 2 — R-slot ultimate:**
- Press R during combat. Confirm: ultimate fires (visual effect), R slot enters cooldown (gray overlay with countdown in seconds), cooldown is 60-120 seconds for the hero you are playing.
- Wait for R to cooldown — confirm the gold border returns when ready.

**Criterion 3 — Passive triggers with VFX:**
- Play any hero and get a kill (on_kill passive), get auto-attacked (on_damage_taken passive), or land auto-attacks (on_hit passive).
- Confirm: each passive trigger shows a gold particle burst + brief hero alpha flash.
- Try a few different heroes if hero selection exists, otherwise confirm for the default hero.

**Criterion 4 — XP bar and leveling:**
- Look at the bottom-left HUD panel. Confirm: thin gold XP bar visible below the mana bar, "LV 1" text beside it.
- Get kills. After 1 kill (50 XP), XP bar should show progress or hero levels to 2.
- After 6-7 kills, hero should reach level 5. The "LEVEL N!" floating text should appear over the hero on each level-up.
- Confirm stats visibly increase: HP bar maximum is higher at level 5 than level 1.

**Criterion 5 — Combat event hooks:**
- No console errors when passive triggers fire (open browser DevTools → Console).
- After a scene restart (Play Again), confirm passives still work and no "Cannot read property" errors appear.

Type "approved" if all 5 criteria pass, or describe any specific failures with the criterion number.
  </how-to-verify>
  <resume-signal>Type "approved" if Phase 2 is complete, or describe issues (e.g. "criterion 3 fails — no VFX on passive trigger")</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 2 success criteria verified by human play:
1. 6+ heroes with distinct roles playable
2. R ultimate fires, gold slot visible, 60-120s cooldown
3. Passives trigger with gold VFX
4. XP bar fills, hero reaches level 5+ in 5-minute match
5. No console errors on passive triggers or scene restart
</verification>

<success_criteria>
Human confirms all 5 Phase 2 success criteria are met in live gameplay.
TypeScript compiles with zero errors.
No console errors during passive triggers or scene restart.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-identity/02-06-SUMMARY.md`

Also update .planning/STATE.md:
- Phase: 2 of 8 (Hero Identity) — COMPLETE
- Status: Phase complete — ready for Phase 3
</output>
