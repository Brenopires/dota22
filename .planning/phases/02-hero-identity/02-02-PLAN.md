---
phase: 02-hero-identity
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/heroes/heroData.ts
  - src/entities/Hero.ts
  - src/scenes/BattleScene.ts
  - src/ai/AIController.ts
autonomous: true

must_haves:
  truths:
    - "All 13 heroes have a 4th ability in their abilities array with slot: 'R' and cooldown 60-120s"
    - "Hero.abilityCooldowns has 4 slots — index [3] tracks R cooldown"
    - "Player can press R key during a match to trigger their hero's ultimate"
    - "AI heroes use their R-slot ultimate at 30% probability when it is ready and they are in ATTACK state"
  artifacts:
    - path: "src/heroes/heroData.ts"
      provides: "13 R-slot ultimate definitions"
      contains: "slot: 'R'"
    - path: "src/entities/Hero.ts"
      provides: "abilityCooldowns expanded to 4 slots"
      contains: "abilityCooldowns: number[] = [0, 0, 0, 0]"
    - path: "src/scenes/BattleScene.ts"
      provides: "R key binding and input handling"
      contains: "KeyCodes.R"
    - path: "src/ai/AIController.ts"
      provides: "AI ultimate usage logic"
      contains: "shouldUseUltimate"
  key_links:
    - from: "src/scenes/BattleScene.ts"
      to: "src/entities/Hero.ts"
      via: "this.player.useAbility(3, worldPoint.x, worldPoint.y)"
      pattern: "useAbility\\(3"
    - from: "src/ai/AIController.ts"
      to: "src/entities/Hero.ts"
      via: "hero.useAbility(3, ...)"
      pattern: "useAbility\\(3"
---

<objective>
Add R-slot ultimate abilities to all 13 heroes and wire the R key input for the player, plus AI ultimate usage. This completes the HERO-02 requirement (Q/W/E/R ability set per hero).

Purpose: Every hero must have a defining high-impact ability on the R slot. The existing CombatSystem.executeAbility() handles all ability types already — this plan only adds data definitions and input wiring.
Output: heroData.ts with 13 R ultimates, expanded abilityCooldowns, R key binding, and AI ultimate logic.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-identity/02-RESEARCH.md
@.planning/phases/02-hero-identity/02-01-SUMMARY.md
@src/heroes/heroData.ts
@src/types.ts
@src/entities/Hero.ts
@src/scenes/BattleScene.ts
@src/ai/AIController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add R-slot ultimate to all 13 heroes in heroData.ts and expand abilityCooldowns</name>
  <files>src/heroes/heroData.ts, src/entities/Hero.ts</files>
  <action>
**Hero.ts:** Change `abilityCooldowns: number[] = [0, 0, 0]` to `abilityCooldowns: number[] = [0, 0, 0, 0]`.

**heroData.ts:** Add a 4th entry (`slot: 'R', isUltimate: true`) to the `abilities` array of ALL 13 heroes. Use `AbilityType` values that already exist. Design each ultimate for high impact with 60–120s cooldown. Keep manaCost 80-150 (heroes must save mana for it).

Ultimate designs (use EXACTLY these — they are calibrated for balance and mechanical distinctness):

**iron_guard** — `{ id: 'ig_r', name: 'Warlord Surge', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 120, cooldown: 90, damage: 200, radius: 200, duration: 2, buffType: BuffType.STUN, buffValue: 0, buffDuration: 2, description: 'Massive AoE shockwave stuns all nearby enemies for 2s' }`

**shadow_blade** — `{ id: 'sb_r', name: 'Death Sentence', slot: 'R', isUltimate: true, type: AbilityType.DASH, manaCost: 100, cooldown: 75, damage: 350, dashDistance: 500, dashSpeed: 1200, buffType: BuffType.SILENCE, buffDuration: 2, description: 'Blinding dash dealing massive damage and silencing target' }`

**flame_witch** — `{ id: 'fw_r', name: 'Inferno', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 150, cooldown: 100, damage: 80, radius: 220, duration: 4, buffType: BuffType.DOT, buffValue: 40, buffDuration: 4, description: 'Summons a fire zone dealing DoT to all enemies inside for 4s' }`

**frost_archer** — `{ id: 'fa_r', name: 'Blizzard', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 130, cooldown: 90, damage: 120, radius: 250, duration: 3, buffType: BuffType.SLOW, buffValue: 0.7, buffDuration: 3, description: 'Freezing storm slows all enemies 70% for 3s' }`

**holy_priest** — `{ id: 'hp_r', name: 'Divine Shield', slot: 'R', isUltimate: true, type: AbilityType.BUFF, manaCost: 120, cooldown: 120, buffType: BuffType.SHIELD, buffValue: 500, buffDuration: 5, radius: 200, description: 'Grants a massive 500HP shield to all nearby allies for 5s' }`

**storm_caller** — `{ id: 'sc_r', name: 'Thunderstorm', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 150, cooldown: 100, damage: 300, radius: 300, duration: 1.5, buffType: BuffType.STUN, buffValue: 0, buffDuration: 1, description: 'Arena-wide lightning strike hitting all enemies' }`

**blade_dancer** — `{ id: 'bd_r', name: 'Whirlwind', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 100, cooldown: 80, damage: 50, radius: 120, duration: 3, healAmount: 25, description: 'Spinning AoE dealing damage per tick with lifesteal' }`

**war_drummer** — `{ id: 'wd_r', name: 'Battle Cry', slot: 'R', isUltimate: true, type: AbilityType.BUFF, manaCost: 120, cooldown: 90, buffType: BuffType.STAT_BUFF, buffValue: 60, buffDuration: 8, radius: 250, description: 'Massive team-wide damage buff for 8s' }`

**venom_stalker** — `{ id: 'vs_r', name: 'Toxic Cloud', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 130, cooldown: 85, damage: 30, radius: 200, duration: 5, buffType: BuffType.DOT, buffValue: 50, buffDuration: 5, description: 'Persistent poison field that infects all enemies inside' }`

**stone_golem** — `{ id: 'sg_r', name: 'Petrify', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 140, cooldown: 110, damage: 250, radius: 180, duration: 2, buffType: BuffType.ROOT, buffValue: 0, buffDuration: 3, description: 'Petrifies all nearby enemies, rooting them for 3s' }`

**lightning_duelist** — `{ id: 'ld_r', name: 'Chain Lightning', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 120, cooldown: 75, damage: 200, radius: 280, duration: 1, description: 'Chain lightning overload hitting all enemies in range' }`

**blood_shaman** — `{ id: 'bs_r', name: 'Blood Sacrifice', slot: 'R', isUltimate: true, type: AbilityType.AREA, manaCost: 80, cooldown: 90, damage: 400, radius: 250, duration: 1, description: 'Sacrifices 20% of own HP to deal massive AoE damage' }`

**phantom_knight** — `{ id: 'pk_r', name: 'Ghost Form', slot: 'R', isUltimate: true, type: AbilityType.SELF_BUFF, manaCost: 100, cooldown: 100, buffType: BuffType.SHIELD, buffValue: 300, buffDuration: 6, description: 'Enters ghost form: massive shield + nearby enemy DoT for 6s' }`

Note on heroData.ts structure: Each hero's `abilities` array must have the R entry added as index [3]. The existing Q/W/E entries at indices [0][1][2] must remain UNCHANGED.

Also: `passive` field must NOT be added here — Plan 02-04 handles that. TypeScript errors on the missing `passive` field are expected and will remain until 02-04.
  </action>
  <verify>
1. Run `npx tsc --noEmit` — errors should still only be missing `passive` on each of the 13 heroes (not AbilityDef slot errors or anything else new).
2. Count: `grep -c "slot: 'R'" src/heroes/heroData.ts` — must output 13.
3. Verify abilityCooldowns: `grep "abilityCooldowns" src/entities/Hero.ts` — must show `[0, 0, 0, 0]`.
  </verify>
  <done>All 13 heroes have an R-slot ability with 60-120s cooldown. Hero.abilityCooldowns is a 4-element array. TypeScript errors are only the expected `passive` field missing (count: 13 errors).</done>
</task>

<task type="auto">
  <name>Task 2: Add R key input for player and AI ultimate logic</name>
  <files>src/scenes/BattleScene.ts, src/ai/AIController.ts</files>
  <action>
**BattleScene.ts:**

1. Add R key to the `keys` object declaration and initialization:
   - In the type declaration (around line 35-46), add: `R: Phaser.Input.Keyboard.Key;`
   - In `create()` where keys are initialized (around line 142-153), add: `R: this.input.keyboard!.addKey(Phaser.Input.Keyboard.KeyCodes.R),`

2. Find `handlePlayerInput()` method (or wherever I/O/P keys are handled — search for `JustDown` or `keys.I`). Inside the block that guards on `this.player && this.player.isAlive`, add the R key handler AFTER the P key handler:
```typescript
if (Phaser.Input.Keyboard.JustDown(this.keys.R)) {
  const worldPoint = this.cameras.main.getWorldPoint(
    this.input.mousePointer.x,
    this.input.mousePointer.y
  );
  this.player.useAbility(3, worldPoint.x, worldPoint.y);
}
```
If the existing I/O/P handlers already compute `worldPoint`, reuse that variable — do not duplicate it.

**AIController.ts:**

Add a `shouldUseUltimate()` private method and integrate it into `shouldUseAbility()`:

```typescript
private shouldUseUltimate(): boolean {
  if (Math.random() > 0.3) return false; // AI uses ult only 30% of chances
  if (!this.hero.stats.abilities[3]) return false;
  if (this.hero.abilityCooldowns[3] > 0) return false;
  if (this.hero.currentMana < this.hero.stats.abilities[3].manaCost) return false;
  return true;
}
```

In `shouldUseAbility()`, add a check at the beginning:
```typescript
if (this.shouldUseUltimate()) return true;
```

In `executeUseAbility()`, find where the AI picks which ability to use (the loop over `stats.abilities`). The loop already iterates `stats.abilities.length` — it will naturally include index 3 now that the hero has 4 abilities. Verify the existing loop handles 4 abilities correctly (check if it uses `abilityCooldowns[i]`); if it does, no changes needed beyond the `shouldUseUltimate` addition. If there is a hardcoded `i < 3`, change it to `i < this.hero.stats.abilities.length`.

PITFALL: The R key must only fire when `player.isAlive` is true. Verify this is inside the isAlive guard. Hero.useAbility() has its own isAlive guard as secondary protection, but the input should not even attempt the call.
  </action>
  <verify>
1. Run `npx tsc --noEmit` — no new errors beyond the expected 13 missing `passive` errors.
2. Search: `grep "KeyCodes.R" src/scenes/BattleScene.ts` — must appear in both the type declaration and create().
3. Search: `grep "shouldUseUltimate\|useAbility(3" src/ai/AIController.ts` — both must appear.
4. Start the game (`npm run dev`), select a hero, press R in combat — no crash, ultimate fires if off-cooldown.
  </verify>
  <done>R key fires player ultimate (slot 3). AI uses ultimate at 30% probability when ready. TypeScript compiles with only expected passive-field errors.</done>
</task>

</tasks>

<verification>
1. `grep -c "slot: 'R'" src/heroes/heroData.ts` outputs 13.
2. `grep "abilityCooldowns" src/entities/Hero.ts` shows `[0, 0, 0, 0]`.
3. `grep "KeyCodes.R" src/scenes/BattleScene.ts` shows 2 hits (declaration + initialization).
4. `npx tsc --noEmit` shows ONLY passive-field errors (13 count), nothing else.
5. Game runs without crash after R key is pressed.
</verification>

<success_criteria>
1. All 13 heroes have a 4th ability with `slot: 'R'` and `isUltimate: true` and cooldown 60-120s.
2. Hero.abilityCooldowns is `[0, 0, 0, 0]` (4 elements).
3. Player presses R → `useAbility(3, x, y)` is called on the player hero.
4. AI calls `useAbility(3, ...)` when `shouldUseUltimate()` returns true.
5. TypeScript compilation has exactly 13 errors — all `passive` field missing in heroData.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-identity/02-02-SUMMARY.md`
</output>
