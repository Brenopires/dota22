---
phase: 04-boss-towers
plan: 06
type: execute
wave: 5
depends_on: ["04-04", "04-05"]
files_modified:
  - src/scenes/BattleScene.ts
autonomous: false

must_haves:
  truths:
    - "Boss spawns at match start and is visibly present at arena center"
    - "Boss aggros, attacks, and transitions through phases when heroes engage it"
    - "Boss kill grants visible stat buff, revival token works, enemy tower disables"
    - "Towers attack nearby enemies and regenerate HP when not under attack"
    - "Destroying a tower ends the match with a victory screen"
    - "Boss health bar and tower indicators update correctly in the HUD"
    - "Match restart (Play Again) cleanly resets all boss/tower state"
  artifacts:
    - path: "src/scenes/BattleScene.ts"
      provides: "Fully integrated boss and tower systems"
  key_links:
    - from: "src/scenes/BattleScene.ts"
      to: "src/entities/BossEntity.ts"
      via: "boss lifecycle: spawn, update, kill, cleanup"
    - from: "src/scenes/BattleScene.ts"
      to: "src/entities/TowerEntity.ts"
      via: "tower lifecycle: spawn, update, destroy, cleanup"
---

<objective>
Human verification checkpoint -- confirm all Phase 4 systems work correctly in live gameplay.

Purpose: Phase 4 introduces two major new entity types and several cross-system mechanics (revival token, tower disable, tower destruction win condition). This checkpoint verifies the complete integration works as intended before marking the phase complete.

Output: Verified Phase 4 or list of issues to address.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-boss-towers/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile and smoke test</name>
  <files>src/scenes/BattleScene.ts</files>
  <action>
Run `npx tsc --noEmit` to verify everything compiles. If there are any type errors, fix them.

Then run `npm run dev` (or equivalent) to start the dev server and verify the game loads without console errors.

Check BattleScene.ts for any issues:
1. Verify boss and tower are properly initialized in create() reset block.
2. Verify all EventBus subscriptions have matching .off() calls in shutdown().
3. Verify no circular import issues.

If any issues found, fix them in BattleScene.ts.
  </action>
  <verify>Run `npx tsc --noEmit` -- zero errors. Run dev server and check browser console for errors on match start.</verify>
  <done>TypeScript compiles with zero errors. Game loads and starts a match without console errors. Boss and towers are visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete boss and tower system: BossEntity with 3-phase FSM and scaling, TowerEntity with AoE attack and regen, boss kill rewards (buff + revival token + tower disable), tower destruction win condition, boss health bar UI, tower status indicators.</what-built>
  <how-to-verify>
    1. Start a match. Verify:
       - Boss (dark red circle with "B" label) is visible at arena center
       - Both towers (grey squares with "T") are visible near each team's spawn
       - Boss health bar appears at top center below timer with "ANCIENT GUARDIAN" label
       - Tower mini health bars appear flanking the kill score

    2. Walk to the boss. Verify:
       - Boss starts attacking you when you enter aggro range
       - Boss deals damage (you see floating damage numbers)
       - Boss returns to center if you run far away

    3. Fight the boss to 60% HP. Verify:
       - "ENRAGED!" text appears
       - Boss health bar color changes (red to orange)
       - Boss attacks faster and hits with AoE

    4. Fight boss to 25% HP. Verify:
       - "DYING!" text appears
       - Boss attacks even faster with larger AoE

    5. Kill the boss. Verify:
       - Kill feed shows boss death
       - "BOSS SLAIN!" banner appears
       - Boss health bar disappears
       - Your team gets a stat buff (damage increase visible)
       - Enemy tower shows "[OFF]" or disabled state in HUD

    6. After boss kill, check revival token:
       - Let yourself die. You should revive at 30% HP with "REVIVED!" text
       - The revival should only work once

    7. Attack the enemy tower. Verify:
       - Tower takes damage (you see damage numbers)
       - Tower attacks you back when you're in range
       - If you back off, tower HP regenerates after a few seconds
       - If you destroy the tower, the match ends immediately with victory

    8. Click "Play Again". Verify:
       - Boss respawns at center
       - Both towers respawn
       - Boss health bar reappears
       - No errors in console
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All Phase 4 success criteria verified in live gameplay:
1. Boss spawns at match start, power increases each minute, engages nearby heroes
2. Boss transitions through Normal -> Enraged -> Dying with distinct attack patterns
3. Boss kill grants team stat buff and revival token
4. Towers damage nearby enemies, regenerate HP, and disable after boss kill
5. Tower destruction immediately ends the match
</verification>

<success_criteria>
All 5 Phase 4 success criteria from the roadmap are verified working:
1. Boss spawns, scales per minute, engages heroes
2. Boss has 3 combat phases with distinct patterns
3. Boss kill grants stat buff + revival token
4. Towers deal AoE, regen HP, disable after boss kill
5. Tower destruction ends the match
</success_criteria>

<output>
After completion, create `.planning/phases/04-boss-towers/04-06-SUMMARY.md`
</output>
