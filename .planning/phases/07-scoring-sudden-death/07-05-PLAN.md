---
phase: 07-scoring-sudden-death
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - .planning/phases/07-scoring-sudden-death/07-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "All 5 success criteria are verified via code inspection and artifact checks"
    - "STATE.md reflects Phase 7 completion"
    - "ROADMAP.md shows Phase 7 as complete with all plans checked"
  artifacts:
    - path: ".planning/phases/07-scoring-sudden-death/07-VERIFICATION.md"
      provides: "Phase 7 success criteria verification results"
    - path: ".planning/STATE.md"
      provides: "Updated project state reflecting Phase 7 completion"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 marked complete with plan checkboxes"
---

<objective>
Verify all 5 Phase 7 success criteria via TypeScript compilation, code artifact inspection, and grep-based checks. Update STATE.md and ROADMAP.md to reflect Phase 7 completion.

Purpose: Formal verification that all success criteria are met before marking the phase as complete. This prevents false completion.

Output: VERIFICATION.md with pass/fail for each criterion, updated STATE.md and ROADMAP.md.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scoring-sudden-death/07-01-SUMMARY.md
@.planning/phases/07-scoring-sudden-death/07-02-SUMMARY.md
@.planning/phases/07-scoring-sudden-death/07-03-SUMMARY.md
@.planning/phases/07-scoring-sudden-death/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 5 success criteria</name>
  <files>.planning/phases/07-scoring-sudden-death/07-VERIFICATION.md</files>
  <action>
Run `npx tsc --noEmit` to confirm zero compile errors.

Then verify each success criterion:

**SC1: HUD shows live score for both teams tracking kills (1pt), boss kills (3pt), tower damage thresholds (2pt), and neutral camp control (1pt).**
- Grep HUD.ts for `getScore` — confirms score polling
- Grep HUD.ts for `scoreBreakdownText` — confirms breakdown display
- Grep MatchStateMachine.ts for `BOSS_KILL_SCORE` — confirms 3pt boss scoring
- Grep MatchStateMachine.ts for `TOWER_DAMAGE_THRESHOLD_SCORE` — confirms 2pt tower scoring
- Grep constants.ts for `KILL_SCORE = 1` and `CAMP_SCORE_POINTS = 1` — confirms 1pt values

**SC2: If score is tied at 5:00, match transitions to Sudden Death — respawns disabled, screen flashes red, HUD shows "SUDDEN DEATH."**
- Grep MatchStateMachine.ts for `timer_tie` — confirms tie check at timer 0
- Grep BattleScene.ts for `respawnTimers.clear()` in SUDDEN_DEATH handler — confirms respawn cancellation
- Grep BattleScene.ts for `screenFlash` in SUDDEN_DEATH handler — confirms red flash
- Grep HUD.ts for `SUDDEN DEATH` — confirms text display

**SC3: Third boss kill triggers Sudden Death immediately, regardless of timer.**
- Grep BattleScene.ts for `bossKillCount >= 3` and `triggerSuddenDeath` — confirms Tier 3 trigger
- Grep BattleScene.ts for `boss_tier3` — confirms reason parameter

**SC4: Second boss kill grants permanent damage amp buff and boss begins roaming.**
- Grep BattleScene.ts for `BOSS_TIER2_DAMAGE_AMP` — confirms damage amp application
- Grep BattleScene.ts for `boss_tier2_reward` — confirms distinct sourceId
- Grep BattleScene.ts for `enableRoaming` — confirms roaming activation
- Grep BossAISystem.ts for `roamToNextWaypoint` — confirms waypoint patrol

**SC5: Match never runs past 5:00 plus pending death animations.**
- Grep MatchStateMachine.ts for `this.phase !== MatchPhase.ACTIVE` at top of onTick — confirms timer stops ticking after ACTIVE
- Grep BattleScene.ts for `endingMatch` guard — confirms no double-fire
- Verify transition() forward-only guard includes SUDDEN_DEATH — prevents re-entry

Write results to `07-VERIFICATION.md` with PASS/FAIL for each criterion.
  </action>
  <verify>07-VERIFICATION.md exists with all 5 criteria marked PASS.</verify>
  <done>All 5 success criteria verified and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and ROADMAP.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
1. Update `.planning/STATE.md`:
   - Phase: 7 of 8 (Scoring & Sudden Death) -- COMPLETE
   - Plan: 5 of 5 -- all done
   - Status: Phase 7 complete -- all 5 success criteria verified
   - Last activity: current date
   - Progress: 87.5% (7/8 phases)
   - Add Phase 7 to velocity table
   - Current focus: Phase 8 -- Draft & Ranked
   - Add Phase 7 decisions to Accumulated Context > Decisions section

2. Update `.planning/ROADMAP.md`:
   - Mark Phase 7 checkbox as complete: `[x]`
   - Update Phase 7 Plans: `**Plans:** 5 plans`
   - Update plan list with checkbox markers `[x]` and actual plan filenames
   - Update progress table: Phase 7 row shows 5/5 Complete with date

3. Key decisions to record in STATE.md:
   - [07-01]: SUDDEN_DEATH inserted at index 2 in MatchPhase enum — forward-only transition guard preserved
   - [07-01]: Tower damage threshold at 50% HP (TOWER_DAMAGE_THRESHOLD_PCT = 0.50) — tunable constant
   - [07-01]: Boss kill scoring uses killerId suffix convention ('_A'/'_B') — same pattern as camp scoring
   - [07-02]: Boss respawns with current-minute scaling (scalePower applied after respawn) — keeps boss relevant
   - [07-02]: Waypoint graph over NavMesh for boss roaming — Phaser colliders handle obstacle sliding
   - [07-02]: Tier 2 damage amp uses sourceId 'boss_tier2_reward' — stacks independently with Tier 1
   - [07-04]: In-flight respawn timers cancelled on SUDDEN_DEATH entry — prevents ghost respawns
   - [07-04]: Team wipe detection in onHeroKilled during SUDDEN_DEATH — triggers ENDED transition
  </action>
  <verify>Grep STATE.md for "Phase 7" and "COMPLETE". Grep ROADMAP.md for "[x] **Phase 7".</verify>
  <done>STATE.md and ROADMAP.md reflect Phase 7 completion with all decisions recorded.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- 07-VERIFICATION.md exists with 5/5 PASS results
- STATE.md shows Phase 7 complete, progress at 87.5%
- ROADMAP.md shows Phase 7 checked off with 5 plans listed
</verification>

<success_criteria>
Phase 7 is formally verified and documented as complete. All project state files are updated. Ready for Phase 8 planning.
</success_criteria>

<output>
After completion, create `.planning/phases/07-scoring-sudden-death/07-05-SUMMARY.md`
</output>
