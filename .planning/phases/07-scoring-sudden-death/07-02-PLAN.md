---
phase: 07-scoring-sudden-death
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/entities/BossEntity.ts
  - src/systems/BossAISystem.ts
  - src/scenes/BattleScene.ts
autonomous: true

must_haves:
  truths:
    - "Boss respawns 30 seconds after being killed (Tier 1 and Tier 2)"
    - "Second boss kill grants the killing team a permanent damage amplification buff (+25 damage)"
    - "After Tier 2, the respawned boss roams the arena via waypoints instead of leashing to home"
    - "Boss roaming uses obstacle-aware movement (Phaser colliders handle sliding along obstacles)"
    - "Boss does NOT respawn after the third kill"
  artifacts:
    - path: "src/entities/BossEntity.ts"
      provides: "respawnBoss() method that resets HP, phase, physics, visibility"
      contains: "respawnBoss"
    - path: "src/systems/BossAISystem.ts"
      provides: "Roaming waypoint patrol mode with enableRoaming() and roamToNextWaypoint()"
      contains: "enableRoaming"
    - path: "src/scenes/BattleScene.ts"
      provides: "bossKillCount tracking, tier-based rewards, boss respawn scheduling"
      contains: "bossKillCount"
  key_links:
    - from: "src/scenes/BattleScene.ts"
      to: "src/entities/BossEntity.ts"
      via: "this.boss.respawnBoss() in scheduleBossRespawn"
      pattern: "boss\\.respawnBoss"
    - from: "src/scenes/BattleScene.ts"
      to: "src/systems/BossAISystem.ts"
      via: "this.bossAI.enableRoaming() after Tier 2 kill"
      pattern: "bossAI\\.enableRoaming"
---

<objective>
Implement boss respawn mechanics, Tier 2 rewards (permanent damage amp + roaming), and boss kill count tracking in BattleScene.

Purpose: The boss must be killable 3 times for the Tier progression system to work. Tier 2 (second kill) is the most mechanically complex addition — it grants a permanent team buff and switches the boss from leash-to-home behavior to arena-wide waypoint roaming.

Output: BossEntity with respawnBoss(), BossAISystem with waypoint roaming, BattleScene with bossKillCount and tier progression logic.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-scoring-sudden-death/07-RESEARCH.md
@.planning/phases/07-scoring-sudden-death/07-01-SUMMARY.md
@src/entities/BossEntity.ts
@src/systems/BossAISystem.ts
@src/scenes/BattleScene.ts
@src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BossEntity respawnBoss() + BossAISystem waypoint roaming</name>
  <files>src/entities/BossEntity.ts, src/systems/BossAISystem.ts</files>
  <action>
**BossEntity.ts — Add respawnBoss() method:**

1. Add a public `respawnBoss()` method that:
   - Sets `this.isAlive = true`
   - Resets `this.currentHP = this.maxHP` (use current maxHP which includes scaling)
   - Resets `this.phase = BossPhase.NORMAL`
   - Resets `this.attackTimer = 0`
   - Re-enables physics body: `body.setEnable(true)`, `body.setCircle(BOSS_RADIUS, -BOSS_RADIUS, -BOSS_RADIUS)`, `body.setVelocity(0, 0)`
   - Makes visible: `this.setVisible(true)`, `this.setAlpha(1)`, `this.setScale(1)`, `this.setAngle(0)`
   - Resets visual tint to NORMAL phase (bossCircle fillStyle 0x8B0000, outerRing strokeStyle 0xff4444)
   - Emits `Events.BOSS_RESPAWNED` with `{ boss: this }`

2. To support visual reset, the `bossCircle` and `outerRing` fields must be accessible. They are already private fields — keep them private but use in respawnBoss().

**BossAISystem.ts — Add waypoint roaming mode:**

1. Import `BOSS_ROAM_WAYPOINTS`, `BOSS_ROAM_SPEED` from constants.

2. Add private fields:
   - `private isRoaming = false;`
   - `private roamWaypointIndex = 0;`
   - `private readonly WAYPOINT_REACH_DIST = 60;`

3. Add public `enableRoaming(): void` method that sets `this.isRoaming = true` and `this.roamWaypointIndex = 0`.

4. Add public `resetForRespawn(): void` method that resets `this.aggroTarget = null`, `this.stickyTargetTimer = 0`. Does NOT reset isRoaming — roaming persists across respawns once enabled.

5. Add private `roamToNextWaypoint(): void` method:
   - Get current waypoint from `BOSS_ROAM_WAYPOINTS[this.roamWaypointIndex]`
   - Calculate distance to waypoint using `Phaser.Math.Distance.Between`
   - If distance < `WAYPOINT_REACH_DIST`, increment `roamWaypointIndex` (modulo waypoint count)
   - Set velocity toward the next waypoint at `BOSS_ROAM_SPEED`

6. Modify `update()`: In the "No heroes in aggro range" else branch (where returnToHome is currently called), add a check: `if (this.isRoaming) { this.roamToNextWaypoint(); return; }` BEFORE `this.returnToHome(dt)`. When roaming and a hero enters aggro range, the boss still targets them (existing aggro logic handles this). When the hero leaves aggro range, the boss resumes roaming instead of returning home.
  </action>
  <verify>Run `npx tsc --noEmit`. Grep for `respawnBoss` in BossEntity.ts and `enableRoaming` in BossAISystem.ts.</verify>
  <done>BossEntity can be respawned to full state, BossAISystem supports waypoint roaming when enabled.</done>
</task>

<task type="auto">
  <name>Task 2: BattleScene boss kill count tracking and tier progression</name>
  <files>src/scenes/BattleScene.ts</files>
  <action>
1. Add private field `private bossKillCount = 0;` to BattleScene class (near revivalTokenTeam).

2. Reset `this.bossKillCount = 0` in `create()` alongside other field resets.

3. Import `BOSS_TIER2_DAMAGE_AMP`, `BOSS_RESPAWN_DELAY`, `MATCH_DURATION` from constants. Import `Events` (already imported).

4. Rewrite `onBossKilled()` to support tier progression:
   ```
   private onBossKilled({ victim, killerId }: { victim: BaseEntity; killerId?: string }): void {
     const killer = this.findHeroById(killerId);
     if (!killer) return;
     const team = killer.team;
     this.bossKillCount++;

     // --- Tier 1 rewards (apply on ALL kills) ---
     // Team-wide stat buff: +20 damage for 60s
     const allies = team === Team.A ? this.teamA : this.teamB;
     for (const ally of allies) {
       if (ally.isAlive) {
         ally.addBuff({
           type: BuffType.STAT_BUFF,
           value: BOSS_KILL_BUFF_DAMAGE,
           duration: BOSS_KILL_BUFF_DURATION,
           remaining: BOSS_KILL_BUFF_DURATION,
           sourceId: 'boss_reward',
         });
       }
     }
     // Revival token
     this.revivalTokenTeam = team;
     // XP reward
     this.xpSystem.awardObjectiveXP(killerId!);
     // Disable enemy tower
     const enemyTower = team === Team.A ? this.towerB : this.towerA;
     if (enemyTower && enemyTower.isAlive) {
       enemyTower.disable(TOWER_DISABLE_DURATION);
     }

     // --- Tier-specific logic ---
     if (this.bossKillCount === 1) {
       // Tier 1: standard rewards only + respawn boss
       this.hud.showKill('TEAM ' + team, 'ANCIENT GUARDIAN');
       this.showBossKillBanner(team);
       this.scheduleBossRespawn();
     } else if (this.bossKillCount === 2) {
       // Tier 2: permanent damage amp + enable roaming + respawn
       for (const ally of allies) {
         if (ally.isAlive) {
           ally.addBuff({
             type: BuffType.STAT_BUFF,
             value: BOSS_TIER2_DAMAGE_AMP,
             duration: MATCH_DURATION,
             remaining: MATCH_DURATION,
             sourceId: 'boss_tier2_reward',
           });
         }
       }
       if (this.bossAI) {
         this.bossAI.enableRoaming();
       }
       this.hud.showKill('TEAM ' + team, 'ANCIENT GUARDIAN (TIER 2)');
       this.showBossKillBanner(team);  // Reuse existing banner
       this.scheduleBossRespawn();
     } else if (this.bossKillCount >= 3) {
       // Tier 3: trigger Sudden Death — boss does NOT respawn
       this.hud.showKill('TEAM ' + team, 'ANCIENT GUARDIAN (TIER 3)');
       this.showBossKillBanner(team);
       this.matchStateMachine.triggerSuddenDeath('boss_tier3');
     }
   }
   ```

5. Add `scheduleBossRespawn()` private method:
   ```
   private scheduleBossRespawn(): void {
     this.time.delayedCall(BOSS_RESPAWN_DELAY, () => {
       if (this.boss && this.matchStateMachine.getPhase() !== MatchPhase.ENDED) {
         this.boss.respawnBoss();
         // Apply current minute scaling to respawned boss
         if (this.bossMinute > 0) {
           this.boss.scalePower(this.bossMinute);
         }
         // Reset AI state for respawn
         if (this.bossAI) {
           this.bossAI.resetForRespawn();
         }
       }
     });
   }
   ```

6. Do NOT import or modify anything related to Sudden Death HUD or respawn guards — those are plan 04.

CRITICAL: The boss respawn scheduling must guard against MatchPhase.ENDED to avoid respawning boss after match ends.
CRITICAL: Use distinct sourceId 'boss_tier2_reward' for Tier 2 buff so it stacks independently with Tier 1 'boss_reward'.
  </action>
  <verify>Run `npx tsc --noEmit`. Grep for `bossKillCount` and `scheduleBossRespawn` in BattleScene.ts. Verify `BOSS_TIER2_DAMAGE_AMP` import exists.</verify>
  <done>BattleScene tracks boss kills, applies tier-appropriate rewards, schedules boss respawn for Tier 1 and 2, and triggers Sudden Death on Tier 3.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- BossEntity.respawnBoss() resets all state (HP, phase, physics, visibility)
- BossAISystem.enableRoaming() switches to waypoint patrol mode
- BossAISystem roamToNextWaypoint() cycles through BOSS_ROAM_WAYPOINTS
- BattleScene.bossKillCount increments on each boss kill
- Tier 1: standard rewards + respawn
- Tier 2: standard rewards + permanent damage amp + enable roaming + respawn
- Tier 3: standard rewards + triggerSuddenDeath, no respawn
</verification>

<success_criteria>
The boss can be killed and respawned twice (Tier 1 then Tier 2). After Tier 2, the boss roams via waypoints. The third kill triggers Sudden Death via matchStateMachine.triggerSuddenDeath(). All code compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/07-scoring-sudden-death/07-02-SUMMARY.md`
</output>
