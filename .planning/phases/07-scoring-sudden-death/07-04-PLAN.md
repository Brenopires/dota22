---
phase: 07-scoring-sudden-death
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/systems/MatchStateMachine.ts
  - src/scenes/BattleScene.ts
  - src/ui/HUD.ts
autonomous: true

must_haves:
  truths:
    - "When the score is tied at 5:00, the match transitions to SUDDEN_DEATH instead of ENDED"
    - "When the score is NOT tied at 5:00, the match ends normally"
    - "During SUDDEN_DEATH, no hero respawns are scheduled"
    - "In-flight respawn timers are cancelled when SUDDEN_DEATH begins"
    - "When all heroes on one team die during SUDDEN_DEATH, the match transitions to ENDED"
    - "The update loop continues running during SUDDEN_DEATH (heroes can fight)"
    - "The match never runs past 5:00 plus pending death animations"
    - "The HUD shows a red screen flash, persistent red border, and SUDDEN DEATH text on entry"
    - "Player respawn overlay is replaced with NO RESPAWNS message during SUDDEN_DEATH"
  artifacts:
    - path: "src/systems/MatchStateMachine.ts"
      provides: "onTick checks score tie at timer 0, routes to SUDDEN_DEATH or ENDED"
      contains: "SUDDEN_DEATH"
    - path: "src/scenes/BattleScene.ts"
      provides: "SUDDEN_DEATH state handler in onMatchStateChange, respawn guard, team wipe detection"
      contains: "SUDDEN_DEATH"
    - path: "src/ui/HUD.ts"
      provides: "showSuddenDeathOverlay() with red border, text, and respawn overlay override"
      contains: "showSuddenDeathOverlay"
  key_links:
    - from: "src/systems/MatchStateMachine.ts"
      to: "MatchPhase.SUDDEN_DEATH"
      via: "onTick timer-zero tie check"
      pattern: "score\\.teamA === this\\.score\\.teamB"
    - from: "src/scenes/BattleScene.ts"
      to: "src/systems/MatchStateMachine.ts"
      via: "getPhase() === MatchPhase.SUDDEN_DEATH guard in onHeroKilled"
      pattern: "MatchPhase\\.SUDDEN_DEATH"
    - from: "src/scenes/BattleScene.ts"
      to: "src/ui/HUD.ts"
      via: "this.hud.showSuddenDeathOverlay() in onMatchStateChange"
      pattern: "showSuddenDeathOverlay"
---

<objective>
Implement both Sudden Death triggers (5:00 tie and boss Tier 3), the respawn cancellation system, the team wipe detection that ends the match, and the Sudden Death HUD overlay (screen flash, red border, "SUDDEN DEATH" text).

Purpose: This is the keystone plan that closes the match lifecycle loop. Sudden Death is the Phase 7 headline feature — it transforms a tied match into a high-stakes no-respawn elimination. Getting the state transitions, respawn guards, and race condition handling right is critical.

Output: Complete Sudden Death system across MatchStateMachine, BattleScene, and HUD.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-scoring-sudden-death/07-RESEARCH.md
@.planning/phases/07-scoring-sudden-death/07-01-SUMMARY.md
@.planning/phases/07-scoring-sudden-death/07-02-SUMMARY.md
@.planning/phases/07-scoring-sudden-death/07-03-SUMMARY.md
@src/systems/MatchStateMachine.ts
@src/scenes/BattleScene.ts
@src/ui/HUD.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sudden Death timer trigger in MatchStateMachine</name>
  <files>src/systems/MatchStateMachine.ts</files>
  <action>
Modify `onTick()` to check for tied score when the timer reaches 0:

```
private onTick(): void {
  if (this.phase !== MatchPhase.ACTIVE) return;
  this.matchTimeRemaining--;
  EventBus.emit(Events.MATCH_TIMER_TICK, { remaining: this.matchTimeRemaining });
  if (this.matchTimeRemaining <= 0) {
    // FLOW-05: tie at 5:00 -> Sudden Death; otherwise end match
    if (this.score.teamA === this.score.teamB) {
      this.triggerSuddenDeath('timer_tie');
    } else {
      this.transition(MatchPhase.ENDED);
    }
  }
}
```

Also update the `onKill` guard to also check for SUDDEN_DEATH:
```
if (this.phase === MatchPhase.ENDED || this.phase === MatchPhase.SUDDEN_DEATH) return;
```

Wait — per research, kills should still be scored during Sudden Death (they matter for the ongoing fight). However, scoring during Sudden Death is pointless since the win condition is last-team-standing, not score. To keep things clean: guard kills on `ENDED` only (current behavior). Kills during SUDDEN_DEATH increment score harmlessly (it just updates the display). Leave the existing guard as-is: `if (this.phase === MatchPhase.ENDED) return;`.

IMPORTANT: The timer event continues to fire during SUDDEN_DEATH but the `if (this.phase !== MatchPhase.ACTIVE) return;` guard at the top of onTick() prevents any further timer processing. This is correct — no changes needed to stop the timer.

CRITICAL: Do NOT add SUDDEN_DEATH to the update() guard in BattleScene — that is Task 2.
  </action>
  <verify>Run `npx tsc --noEmit`. Grep for `timer_tie` in MatchStateMachine.ts. Verify the onTick score-tie check exists.</verify>
  <done>MatchStateMachine routes to SUDDEN_DEATH on timer tie and ENDED on non-tie.</done>
</task>

<task type="auto">
  <name>Task 2: BattleScene Sudden Death state handler + respawn guard + team wipe detection</name>
  <files>src/scenes/BattleScene.ts</files>
  <action>
1. Import `SUDDEN_DEATH_COLOR`, `SUDDEN_DEATH_FLASH_DURATION`, `SUDDEN_DEATH_FLASH_INTENSITY` from constants.

2. Modify `onMatchStateChange()` to handle SUDDEN_DEATH:
   ```
   private onMatchStateChange({ phase }: { phase: MatchPhase }): void {
     if (phase === MatchPhase.SUDDEN_DEATH) {
       // Cancel all in-flight respawn timers (Pitfall 1 from research)
       for (const timer of this.respawnTimers.values()) {
         this.time.removeEvent(timer);
       }
       this.respawnTimers.clear();
       this.playerRespawnEndTime = 0;

       // Red screen flash
       this.vfxManager.screenFlash(SUDDEN_DEATH_COLOR, SUDDEN_DEATH_FLASH_DURATION, SUDDEN_DEATH_FLASH_INTENSITY);

       // Show Sudden Death HUD overlay
       this.hud.showSuddenDeathOverlay();
     } else if (phase === MatchPhase.ENDED) {
       this.endMatch();
     }
   }
   ```

3. Modify `onHeroKilled()` to add SUDDEN_DEATH respawn guard and team wipe detection. After the kill counting section (after `this.hud.showKill(...)` line), BEFORE the respawn scheduling:
   ```
   // SUDDEN DEATH: no respawns, check for team wipe
   if (this.matchStateMachine.getPhase() === MatchPhase.SUDDEN_DEATH) {
     const teamAAllDead = this.teamA.every(h => !h.isAlive);
     const teamBAllDead = this.teamB.every(h => !h.isAlive);
     if (teamAAllDead || teamBAllDead) {
       this.matchStateMachine.transition(MatchPhase.ENDED);
     }
     return; // No respawn scheduling in Sudden Death
   }
   ```

4. CRITICAL: Do NOT add SUDDEN_DEATH to the `update()` guard. The current guard is `if (this.matchStateMachine?.getPhase() === MatchPhase.ENDED) return;`. During SUDDEN_DEATH, heroes must continue to fight. Only ENDED stops the update loop.

5. Modify `endMatch()` to also handle Sudden Death win condition. The existing logic checks `teamAAlive vs teamBAlive` and falls through to kills comparison. For Sudden Death endings (where one team is wiped), the alive check already works correctly. No changes needed to endMatch() — it already handles the case where one team has 0 alive. The score-based tiebreaker also works as a fallback.

6. Add a guard in `scheduleBossRespawn()` (from plan 02) to also check for SUDDEN_DEATH:
   ```
   if (this.matchStateMachine.getPhase() === MatchPhase.ENDED ||
       this.matchStateMachine.getPhase() === MatchPhase.SUDDEN_DEATH) {
     return; // Don't respawn boss after Sudden Death
   }
   ```
   Wait — scheduleBossRespawn already has a delayedCall that checks phase. But the delayedCall might fire after SD starts. The guard should be inside the delayedCall callback, not outside. Plan 02 already placed `this.matchStateMachine.getPhase() !== MatchPhase.ENDED` inside the callback. Update it to also check `!== MatchPhase.SUDDEN_DEATH`:
   ```
   if (this.boss &&
       this.matchStateMachine.getPhase() !== MatchPhase.ENDED &&
       this.matchStateMachine.getPhase() !== MatchPhase.SUDDEN_DEATH) {
   ```
  </action>
  <verify>Run `npx tsc --noEmit`. Grep for `SUDDEN_DEATH` in BattleScene.ts — should appear in onMatchStateChange, onHeroKilled, and scheduleBossRespawn. Verify respawnTimers.clear() exists in the SUDDEN_DEATH handler.</verify>
  <done>BattleScene handles SUDDEN_DEATH state: cancels respawn timers, shows overlay, blocks new respawns, detects team wipe to end match.</done>
</task>

<task type="auto">
  <name>Task 3: HUD Sudden Death overlay + respawn override</name>
  <files>src/ui/HUD.ts</files>
  <action>
1. Import `GAME_WIDTH`, `GAME_HEIGHT` (already imported), `SUDDEN_DEATH_COLOR` from constants.

2. Add a private field `private isSuddenDeath = false;`.

3. Add a public `showSuddenDeathOverlay(): void` method:
   - Set `this.isSuddenDeath = true;`
   - Draw persistent red border: Create a Graphics object, `lineStyle(6, SUDDEN_DEATH_COLOR, 0.8)`, `strokeRect(3, 3, GAME_WIDTH - 6, GAME_HEIGHT - 6)`, `setScrollFactor(0).setDepth(295)`. Store reference for cleanup.
   - Create "SUDDEN DEATH" text at `(GAME_WIDTH / 2, 56)` — 22px bold red, replace the score text position. Actually, place it at `(GAME_WIDTH / 2, 110)` below the score breakdown to avoid overlap with the timer. Use: `fontSize: '22px', color: '#ff0000', fontFamily: 'monospace', fontStyle: 'bold', stroke: '#000000', strokeThickness: 3`. Set `setScrollFactor(0).setDepth(296)`.
   - Create "NO RESPAWNS" warning text at `(GAME_WIDTH / 2, 134)` — 12px, color `#ff6666`. Set `setScrollFactor(0).setDepth(296)`.

4. In the `update()` method, modify the respawn overlay section:
   - When `this.isSuddenDeath` is true and player is dead, show "ELIMINATED" overlay instead of respawn countdown:
     ```
     if (player && !player.isAlive) {
       if (this.isSuddenDeath) {
         // Show elimination overlay (no countdown)
         if (!this.respawnOverlay) {
           this.respawnOverlay = scene.add.container(GAME_WIDTH / 2, GAME_HEIGHT / 2);
           this.respawnOverlay.setScrollFactor(0).setDepth(290);
           const bg = scene.add.graphics();
           bg.fillStyle(0x000000, 0.7);
           bg.fillRoundedRect(-120, -40, 240, 80, 12);
           this.respawnOverlay.add(bg);
           const label = scene.add.text(0, -10, 'ELIMINATED', {
             fontSize: '28px', color: '#ff0000', fontFamily: 'monospace', fontStyle: 'bold',
             stroke: '#000000', strokeThickness: 3,
           }).setOrigin(0.5);
           this.respawnOverlay.add(label);
           const sub = scene.add.text(0, 20, 'No respawns in Sudden Death', {
             fontSize: '11px', color: '#ff6666', fontFamily: 'monospace',
           }).setOrigin(0.5);
           this.respawnOverlay.add(sub);
         }
       } else {
         // Existing respawn countdown logic (unchanged)
         ...
       }
     }
     ```

5. In the timer display section of `update()`, when `isSuddenDeath` is true, set the timer text to "0:00" and keep it red. The timer is already <= 0 during SD, so `Math.max(0, ...)` handles this. But if SD was triggered by boss Tier 3 (timer might not be 0), also handle that case: timer display should show remaining time normally — it just won't count down since onTick returns early for non-ACTIVE phases.
  </action>
  <verify>Run `npx tsc --noEmit`. Grep for `showSuddenDeathOverlay` and `ELIMINATED` in HUD.ts. Verify `isSuddenDeath` flag is used in update().</verify>
  <done>HUD shows red border, "SUDDEN DEATH" text, and "NO RESPAWNS" warning on Sudden Death entry. Dead player sees "ELIMINATED" instead of respawn countdown.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- MatchStateMachine.onTick routes to SUDDEN_DEATH on tie, ENDED on non-tie
- BattleScene cancels in-flight respawn timers on SUDDEN_DEATH entry
- BattleScene blocks new respawn scheduling during SUDDEN_DEATH
- BattleScene detects team wipe and transitions to ENDED
- BattleScene update() loop continues running during SUDDEN_DEATH
- HUD shows red flash + border + "SUDDEN DEATH" text + "NO RESPAWNS" warning
- HUD shows "ELIMINATED" overlay for dead player instead of respawn countdown
- Boss respawn is blocked during SUDDEN_DEATH
</verification>

<success_criteria>
Both Sudden Death triggers work (5:00 tie and boss Tier 3). Respawns are disabled. The match ends when one team is wiped. The HUD clearly communicates Sudden Death state. No race conditions at the 5:00 boundary. Satisfies success criteria 2, 3, and 5.
</success_criteria>

<output>
After completion, create `.planning/phases/07-scoring-sudden-death/07-04-SUMMARY.md`
</output>
