---
phase: 07-scoring-sudden-death
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/constants.ts
  - src/systems/EventBus.ts
  - src/systems/MatchStateMachine.ts
autonomous: true

must_haves:
  truths:
    - "MatchPhase enum includes SUDDEN_DEATH between ACTIVE and ENDED"
    - "MatchStateMachine transition() guard supports forward-only ordering with SUDDEN_DEATH"
    - "Boss kills award 3 points to the killing team"
    - "Tower damage threshold at 50% HP awards 2 points (one-time per tower)"
    - "SCORE_UPDATED event fires with full breakdown including boss kills and tower thresholds"
  artifacts:
    - path: "src/types.ts"
      provides: "MatchPhase.SUDDEN_DEATH enum value"
      contains: "SUDDEN_DEATH"
    - path: "src/constants.ts"
      provides: "Phase 7 scoring, boss tier 2, sudden death, and roaming constants"
      contains: "BOSS_KILL_SCORE"
    - path: "src/systems/EventBus.ts"
      provides: "SCORE_UPDATED, TOWER_THRESHOLD_SCORED, SUDDEN_DEATH_TRIGGERED events"
      contains: "SUDDEN_DEATH_TRIGGERED"
    - path: "src/systems/MatchStateMachine.ts"
      provides: "Four-source scoring, SUDDEN_DEATH state support, boss kill + tower threshold handlers"
      exports: ["MatchStateMachine"]
  key_links:
    - from: "src/systems/MatchStateMachine.ts"
      to: "Events.BOSS_KILLED"
      via: "EventBus.on in start()"
      pattern: "EventBus\\.on\\(Events\\.BOSS_KILLED"
    - from: "src/systems/MatchStateMachine.ts"
      to: "Events.TOWER_DAMAGED"
      via: "EventBus.on in start()"
      pattern: "EventBus\\.on\\(Events\\.TOWER_DAMAGED"
---

<objective>
Add the SUDDEN_DEATH state to the match phase enum, define all Phase 7 constants and events, and extend MatchStateMachine to score four objective types (kills, boss kills, tower thresholds, camp clears).

Purpose: Every subsequent Phase 7 plan depends on these type definitions, constants, events, and scoring infrastructure. This is the foundation that wires boss kills and tower damage thresholds into the unified score.

Output: Updated types.ts, constants.ts, EventBus.ts, and MatchStateMachine.ts with full four-source scoring.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scoring-sudden-death/07-RESEARCH.md
@src/types.ts
@src/constants.ts
@src/systems/EventBus.ts
@src/systems/MatchStateMachine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SUDDEN_DEATH to MatchPhase + Phase 7 constants + events</name>
  <files>src/types.ts, src/constants.ts, src/systems/EventBus.ts</files>
  <action>
1. In `src/types.ts`, insert `SUDDEN_DEATH = 'sudden_death'` into the MatchPhase enum between ACTIVE and ENDED:
   ```
   export enum MatchPhase {
     PRE_MATCH    = 'pre_match',
     ACTIVE       = 'active',
     SUDDEN_DEATH = 'sudden_death',
     ENDED        = 'ended',
   }
   ```

2. In `src/constants.ts`, add a new "Phase 7" section with these constants:
   ```
   // Phase 7 — Scoring
   export const KILL_SCORE = 1;
   export const BOSS_KILL_SCORE = 3;
   export const TOWER_DAMAGE_THRESHOLD_SCORE = 2;
   export const TOWER_DAMAGE_THRESHOLD_PCT = 0.50;

   // Phase 7 — Boss Tier 2
   export const BOSS_TIER2_DAMAGE_AMP = 25;
   export const BOSS_RESPAWN_DELAY = 30000;
   export const BOSS_ROAM_SPEED = 60;
   export const BOSS_ROAM_WAYPOINTS: { x: number; y: number }[] = [
     { x: 800, y: 200 },
     { x: 1150, y: 350 },
     { x: 1250, y: 600 },
     { x: 1150, y: 850 },
     { x: 800, y: 1000 },
     { x: 450, y: 850 },
     { x: 350, y: 600 },
     { x: 450, y: 350 },
   ];

   // Phase 7 — Sudden Death
   export const SUDDEN_DEATH_COLOR = 0xff0000;
   export const SUDDEN_DEATH_FLASH_DURATION = 600;
   export const SUDDEN_DEATH_FLASH_INTENSITY = 0.7;
   ```

3. In `src/systems/EventBus.ts`, add these events to the Events object:
   ```
   // Phase 7 additions:
   TOWER_THRESHOLD_SCORED: 'tower:threshold_scored',
   SUDDEN_DEATH_TRIGGERED: 'sudden_death:triggered',
   BOSS_RESPAWNED: 'boss:respawned',
   ```
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Grep for `SUDDEN_DEATH` in types.ts, `BOSS_KILL_SCORE` in constants.ts, `SUDDEN_DEATH_TRIGGERED` in EventBus.ts.</verify>
  <done>MatchPhase.SUDDEN_DEATH exists, all Phase 7 constants defined, all new events registered.</done>
</task>

<task type="auto">
  <name>Task 2: Extend MatchStateMachine with four-source scoring and SUDDEN_DEATH support</name>
  <files>src/systems/MatchStateMachine.ts</files>
  <action>
1. Update the `transition()` method's order array to include SUDDEN_DEATH:
   ```
   const order = [MatchPhase.PRE_MATCH, MatchPhase.ACTIVE, MatchPhase.SUDDEN_DEATH, MatchPhase.ENDED];
   ```

2. Extend the `score` object with boss kill tracking and tower threshold booleans:
   ```
   private score = {
     teamA: 0, teamB: 0,
     campClearsA: 0, campClearsB: 0,
     bossKillsA: 0, bossKillsB: 0,
     towerThresholdA: false,
     towerThresholdB: false,
   };
   ```

3. In `start()`, subscribe to `Events.BOSS_KILLED` and `Events.TOWER_DAMAGED`:
   ```
   EventBus.on(Events.BOSS_KILLED, this.onBossKilled, this);
   EventBus.on(Events.TOWER_DAMAGED, this.onTowerDamaged, this);
   ```

4. Add `onBossKilled` handler — use killerId suffix '_A'/'_B' to determine team (same pattern as onCampCleared). Award `BOSS_KILL_SCORE` points. Increment `bossKillsA`/`bossKillsB`. Emit `SCORE_UPDATED`. Guard on `this.phase === MatchPhase.ENDED`.

5. Add `onTowerDamaged` handler — receive `{ tower, damage }` payload. Check `tower.isAlive` first (avoid scoring on destruction frame). Compute `hpRatio = tower.currentHP / tower.maxHP`. If tower.team is A and `!this.score.towerThresholdA` and `hpRatio <= TOWER_DAMAGE_THRESHOLD_PCT`: award `TOWER_DAMAGE_THRESHOLD_SCORE` to teamB, set `towerThresholdA = true`, emit `TOWER_THRESHOLD_SCORED` with `{ scoringTeam: Team.B, tower }`, emit `SCORE_UPDATED`. Same logic for tower.team B (awarding to teamA). Guard on `this.phase === MatchPhase.ENDED`.

6. Update `getScore()` return type to include the new fields:
   ```
   getScore() {
     return { ...this.score };
   }
   ```

7. Add a public `triggerSuddenDeath(reason: string)` method that calls `this.transition(MatchPhase.SUDDEN_DEATH)` and emits `Events.SUDDEN_DEATH_TRIGGERED` with `{ reason }`. This method will be called by BattleScene in plan 04.

8. In `destroy()`, unsubscribe from `Events.BOSS_KILLED` and `Events.TOWER_DAMAGED`.

9. Import `BOSS_KILL_SCORE`, `TOWER_DAMAGE_THRESHOLD_SCORE`, `TOWER_DAMAGE_THRESHOLD_PCT` from constants. Import `Team` from types.

IMPORTANT: Do NOT modify `onTick()` yet — the Sudden Death timer trigger is plan 04. The current `onTick()` should continue to transition to ENDED when timer reaches 0. Plan 04 will modify it.

IMPORTANT: The `onKill` handler's existing guard `if (this.phase === MatchPhase.ENDED) return;` is sufficient — during SUDDEN_DEATH, kills should still be scored.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Grep for `onBossKilled` and `onTowerDamaged` in MatchStateMachine.ts. Verify `SUDDEN_DEATH` appears in the transition order array.</verify>
  <done>MatchStateMachine scores boss kills (3pt) and tower thresholds (2pt), supports SUDDEN_DEATH in transition guard, and exposes triggerSuddenDeath().</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- MatchPhase enum has exactly 4 values: PRE_MATCH, ACTIVE, SUDDEN_DEATH, ENDED
- All Phase 7 constants are importable from constants.ts
- All new events are in EventBus.ts Events object
- MatchStateMachine.score includes bossKillsA, bossKillsB, towerThresholdA, towerThresholdB
- MatchStateMachine.transition() order array has 4 entries in correct order
- MatchStateMachine.triggerSuddenDeath() exists and emits SUDDEN_DEATH_TRIGGERED
</verification>

<success_criteria>
The four-source scoring system compiles, boss kills and tower damage thresholds are wired into MatchStateMachine via EventBus, and the SUDDEN_DEATH state is recognized by the transition guard.
</success_criteria>

<output>
After completion, create `.planning/phases/07-scoring-sudden-death/07-01-SUMMARY.md`
</output>
