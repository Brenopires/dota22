---
phase: 07-scoring-sudden-death
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/ui/HUD.ts
autonomous: true

must_haves:
  truths:
    - "HUD shows a live scoreboard with total score for both teams"
    - "Scoreboard includes a breakdown showing kills, boss kills, tower thresholds, and camp clears"
    - "Tower threshold scoring shows a visual cue in the tower indicator when triggered"
  artifacts:
    - path: "src/ui/HUD.ts"
      provides: "Live scoreboard replacing kill-only display, tower threshold visual cue"
      contains: "scoreBreakdownText"
  key_links:
    - from: "src/ui/HUD.ts"
      to: "src/systems/MatchStateMachine.ts"
      via: "matchStateMachine.getScore() polled each frame"
      pattern: "matchStateMachine\\.getScore"
---

<objective>
Replace the kill-only score display in the HUD with a live four-source scoreboard showing total scores and a compact breakdown, and add a visual cue on the tower indicator when a tower damage threshold is triggered.

Purpose: Success criterion 1 requires the HUD to show a live score tracking kills (1pt), boss kills (3pt), tower damage thresholds (2pt), and neutral camp control (1pt). This makes objective-focused play visible and strategic.

Output: Updated HUD.ts with live scoreboard and tower threshold visual feedback.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-scoring-sudden-death/07-RESEARCH.md
@.planning/phases/07-scoring-sudden-death/07-01-SUMMARY.md
@src/ui/HUD.ts
@src/systems/MatchStateMachine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace kill score with live four-source scoreboard</name>
  <files>src/ui/HUD.ts</files>
  <action>
1. Replace the `killsText` field with a `scoreText` field (total score display) and add a `scoreBreakdownText` field (compact breakdown below total).

2. In the constructor, replace the kill score text creation:
   - `scoreText` at `(GAME_WIDTH / 2, 56)` — larger font, showing `"0 - 0"` (total score).
   - `scoreBreakdownText` at `(GAME_WIDTH / 2, 92)` — small 9px text below the trait indicator, showing `"K:0 B:0 T:0 C:0 | K:0 B:0 T:0 C:0"` where K=kills, B=boss, T=tower, C=camps. This goes below the trait indicator (which is at y=76). Use color `#666666` for subtle display.

3. In `update()`, replace the kills display logic:
   - Get the full score object: `const score = scene.matchStateMachine?.getScore()` (the extended score with bossKillsA, bossKillsB, towerThresholdA, towerThresholdB, campClearsA, campClearsB).
   - If score is available:
     - `scoreText.setText(`${score.teamA} - ${score.teamB}`)` for total score.
     - Calculate per-source breakdown: kills from `scene.teamAKills`/`scene.teamBKills`, boss kills from `score.bossKillsA`/`score.bossKillsB`, tower thresholds from `score.towerThresholdA ? 1 : 0` / `score.towerThresholdB ? 1 : 0`, camp clears from `score.campClearsA`/`score.campClearsB`.
     - Set breakdown text: `"K:${killsA} B:${bossA} T:${twrA} C:${campA}  |  K:${killsB} B:${bossB} T:${twrB} C:${campB}"`.
   - Color code: if player is team A, color the left side slightly blue (#5588cc), right side slightly red (#cc5555). If team B, reverse.

4. In the `updateTowerIndicator()` method, add a visual cue when tower threshold has been scored:
   - Accept an additional `thresholdTriggered: boolean` parameter.
   - When `thresholdTriggered` is true and the tower is still alive, add a gold/yellow accent line below the tower bar (or change the tower text to include "[!2pt]" suffix in gold color).
   - In `update()`, pass the threshold booleans from the score object to the tower indicator calls:
     ```
     this.updateTowerIndicator(battleScene.towerA, ..., score?.towerThresholdA ?? false);
     this.updateTowerIndicator(battleScene.towerB, ..., score?.towerThresholdB ?? false);
     ```

5. Shift the score breakdown text y-position if the trait indicator exists vs not. If no trait indicator, place breakdown at y=76 (where trait would be). If trait exists, place at y=92.

IMPORTANT: The `showKill()` method is unchanged — it's the kill feed on the right side, not the score display.
IMPORTANT: Keep the existing timer, HUD structure, and camp buff icons unchanged.
  </action>
  <verify>Run `npx tsc --noEmit`. Grep for `scoreBreakdownText` and `getScore` in HUD.ts. Verify the old `killsText` references are fully replaced.</verify>
  <done>HUD shows total team scores and a compact per-source breakdown. Tower indicators show threshold visual cue when triggered.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- HUD shows total score (not just kills) at top center below timer
- Score breakdown shows K/B/T/C for both teams
- Tower indicators visually reflect when damage threshold has been scored
- Kill feed still works independently on the right side
</verification>

<success_criteria>
The HUD shows a live score for both teams tracking kills (1pt), boss kills (3pt), tower damage thresholds (2pt), and neutral camp control (1pt), satisfying Phase 7 success criterion 1.
</success_criteria>

<output>
After completion, create `.planning/phases/07-scoring-sudden-death/07-03-SUMMARY.md`
</output>
