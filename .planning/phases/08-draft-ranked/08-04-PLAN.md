---
phase: 08-draft-ranked
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "All 4 Phase 8 success criteria verified via code inspection"
    - "STATE.md updated with Phase 8 complete"
    - "ROADMAP.md updated with Phase 8 complete and plan list"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated project state"
      contains: "Phase 8"
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap with Phase 8 complete"
      contains: "[x] **Phase 8"
  key_links: []
---

<objective>
Verify all 4 Phase 8 success criteria via code inspection (tsc + grep artifact checks), then update STATE.md and ROADMAP.md to mark Phase 8 as complete.

Purpose: Final verification ensures all Phase 8 requirements are met before marking the phase done.
Output: Verified phase completion, updated project tracking files.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-draft-ranked/08-01-SUMMARY.md
@.planning/phases/08-draft-ranked/08-02-SUMMARY.md
@.planning/phases/08-draft-ranked/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 4 Phase 8 success criteria</name>
  <files>src/scenes/DraftScene.ts, src/utils/MMRCalculator.ts, src/constants.ts, src/scenes/MenuScene.ts, src/scenes/ResultScene.ts, src/systems/MatchOrchestrator.ts</files>
  <action>
Run `npx tsc --noEmit` to confirm full codebase compiles.

Then verify each success criterion via grep/code inspection:

**SC1: "The draft scene presents exactly 3 random heroes; the player picks one and that hero is used in the match."**
- Grep DraftScene.ts for `pool.slice(0, 3)` or similar 3-candidate logic
- Grep for `_onCardClicked` (pick handler exists)
- Grep for `finalizeMatch` call with the picked heroId
- Grep for `scene.start.*BattleScene.*matchConfig` (transition passes config)
- PASS if: All patterns found, confirming 3 candidates presented, pick triggers finalizeMatch, matchConfig passed to BattleScene

**SC2: "After each match, MMR changes by exactly ±40 points."**
- Grep MMRCalculator.ts for `return won ? 40 : -40` (flat logic)
- Grep for `if (draw) return 0` (draw handling)
- Grep BattleScene.ts for `MMRCalculator.calculate` (still called in endMatch)
- Grep StorageManager.ts for `data.mmr += result.mmrChange` (applies the ±40)
- PASS if: MMRCalculator returns flat values, BattleScene calls it, StorageManager applies it

**SC3: "The player's current rank tier (Bronze, Silver, Gold, Platinum, Apex) is visible in the main menu and results screen based on their MMR."**
- Grep constants.ts for `Apex` and count RANK_THRESHOLDS entries (must be 5)
- Grep for `Diamond` or `Master` — must return 0 matches
- Grep MenuScene.ts for `getRank` import from RankUtils
- Grep ResultScene.ts for `getRank` import from RankUtils
- Grep MenuScene.ts for `rank.name` display
- Grep ResultScene.ts for `rank.name` display
- PASS if: 5 tiers with Apex, no Diamond/Master, both scenes import and display rank

**SC4: "The draft completes and transitions to BattleScene in under 30 seconds, passing the correct hero and trait selection."**
- Grep constants.ts for `DRAFT_PICK_TIMEOUT = 25` (25s < 30s ceiling)
- Grep DraftScene.ts for `DRAFT_PICK_TIMEOUT` usage (countdown uses this value)
- Grep DraftScene.ts for `traitId` (trait selected during partial generation and passed through)
- Grep MatchOrchestrator.ts for `traitId: partial.traitId` (trait passes into final MatchConfig)
- Grep DraftScene.ts for `fadeOut.*400` (transition animation ~400ms)
- PASS if: Timeout is 25s, countdown uses it, trait is part of the config, fadeOut is fast

Report results as:
```
SC1: [PASS/FAIL] — [evidence]
SC2: [PASS/FAIL] — [evidence]
SC3: [PASS/FAIL] — [evidence]
SC4: [PASS/FAIL] — [evidence]
```

All 4 must PASS to proceed to Task 2. If any FAIL, diagnose and fix inline.
  </action>
  <verify>
Run `npx tsc --noEmit` — must exit 0. Run `grep -c "pool.slice(0, 3)" src/scenes/DraftScene.ts` — must return >= 1 (SC1). Run `grep -c "return won ? 40 : -40" src/utils/MMRCalculator.ts` — must return >= 1 (SC2). Run `grep -c "Apex" src/constants.ts` — must return >= 1, and `grep -c "Diamond\|Master" src/constants.ts` — must return 0 (SC3). Run `grep -c "DRAFT_PICK_TIMEOUT = 25" src/constants.ts` — must return >= 1, and `grep -c "traitId" src/scenes/DraftScene.ts` — must return >= 1 (SC4).
  </verify>
  <done>4/4 Phase 8 success criteria verified as PASS with runnable grep evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and ROADMAP.md for Phase 8 completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**STATE.md updates:**
1. Update "Current Position" section:
   - Phase: 8 of 8 (Draft & Ranked) — COMPLETE
   - Plan: 4 of 4 — all done
   - Status: Phase 8 complete — all 4 success criteria verified
   - Last activity: current date — Completed 08-04: Phase 8 verification + STATE/ROADMAP update
   - Progress: [████████████████████████] 100%

2. Update "Current focus" to: Phase 8 — Draft & Ranked — COMPLETE (all phases done)

3. Add Phase 8 to Performance Metrics table:
   - | 08-draft-ranked | 4/4 | {total} min | {avg} min |

4. Update velocity totals.

5. Add any Phase 8 decisions to the Decisions section.

**ROADMAP.md updates:**
1. Change Phase 8 checkbox from `[ ]` to `[x]`
2. Update the phase line: `- [x] **Phase 8: Draft & Ranked** - Pick-from-3 draft UI, rank tier ladder, ±40 MMR shifts`
3. Update Plans count: `**Plans:** 4 plans`
4. Update plan list:
```
Plans:
- [x] 08-01-PLAN.md — Rank tier fix (5 tiers) + flat ±40 MMR + getRank utility
- [x] 08-02-PLAN.md — MatchOrchestrator partial/finalize split for draft flow
- [x] 08-03-PLAN.md — DraftScene pick-from-3 interactive refactor
- [x] 08-04-PLAN.md — Phase 8 verification + STATE/ROADMAP completion
```
5. Update Progress table: Phase 8 row → 4/4, Complete, current date
  </action>
  <verify>
Grep STATE.md for "Phase 8" and "COMPLETE". Grep ROADMAP.md for "[x] **Phase 8". Grep ROADMAP.md for "08-01-PLAN.md" through "08-04-PLAN.md" with [x] markers.
  </verify>
  <done>
STATE.md shows Phase 8 complete with 100% progress. ROADMAP.md shows Phase 8 checked off with all 4 plans listed as complete.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. All 4 success criteria verified as PASS
3. STATE.md updated with Phase 8 completion
4. ROADMAP.md updated with Phase 8 completion
5. All 8 phases marked complete in ROADMAP.md
</verification>

<success_criteria>
- 4/4 Phase 8 success criteria verified
- STATE.md reflects Phase 8 complete, 100% project progress
- ROADMAP.md has Phase 8 checked off with all plan entries
- TypeScript compiles with zero errors across the entire codebase
</success_criteria>

<output>
After completion, create `.planning/phases/08-draft-ranked/08-04-SUMMARY.md`
</output>