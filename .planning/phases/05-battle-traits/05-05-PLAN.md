---
phase: 05-battle-traits
plan: 05
type: execute
wave: 4
depends_on: ["05-03", "05-04"]
files_modified:
  - .planning/phases/05-battle-traits/05-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "TypeScript compiles with zero errors across all Phase 5 files"
    - "8 traits exist in traitData.ts covering 3 stat, 4 mechanic, 1 rule_change categories"
    - "8 gems exist in gemData.ts with flat stat bonuses only"
    - "MatchOrchestrator returns traitId and gemAssignments in MatchConfig"
    - "TraitSystem subscribes to EventBus and applies mechanic trait effects"
    - "DraftScene displays trait banner and per-hero gem info"
    - "HUD displays trait indicator and player gem indicator"
    - "No CDR gems exist"
    - "Incompatibility blacklists prevent vampiric_pact+bd_passive, executioner+ld_passive, spell_burn+fw/vs_passive"
  artifacts:
    - path: ".planning/phases/05-battle-traits/05-VERIFICATION.md"
      provides: "Phase 5 verification report confirming all success criteria"
      contains: "PASS"
  key_links:
    - from: "src/systems/MatchOrchestrator.ts"
      to: "src/traits/traitData.ts"
      via: "selectTrait uses TRAITS array"
      pattern: "selectTrait"
    - from: "src/scenes/BattleScene.ts"
      to: "src/traits/TraitSystem.ts"
      via: "instantiates and destroys TraitSystem"
      pattern: "traitSystem"
---

<objective>
Verify all Phase 5 success criteria through code inspection and TypeScript compilation, producing a verification report.

Purpose: Confirms the entire Phase 5 implementation is complete and correct before moving to Phase 6. Checks all 4 success criteria from the roadmap plus structural integrity (types compile, no stale imports, cleanup works).

Output: 05-VERIFICATION.md with pass/fail status for each criterion.
</objective>

<execution_context>
@/Users/brenopires/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brenopires/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-battle-traits/05-RESEARCH.md
@.planning/phases/05-battle-traits/05-01-SUMMARY.md
@.planning/phases/05-battle-traits/05-02-SUMMARY.md
@.planning/phases/05-battle-traits/05-03-SUMMARY.md
@.planning/phases/05-battle-traits/05-04-SUMMARY.md
@src/types.ts
@src/traits/traitData.ts
@src/traits/TraitSystem.ts
@src/gems/gemData.ts
@src/systems/MatchOrchestrator.ts
@src/scenes/BattleScene.ts
@src/scenes/DraftScene.ts
@src/ui/HUD.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TypeScript compilation and artifact checks</name>
  <files>.planning/phases/05-battle-traits/05-VERIFICATION.md</files>
  <action>
Run the following verification checks and produce a report:

**Check 1: TypeScript compilation**
```bash
npx tsc --noEmit
```
Must pass with zero errors.

**Check 2: Trait count and categories**
```bash
grep -c "category:" src/traits/traitData.ts
grep "category: 'stat'" src/traits/traitData.ts | wc -l    # expect 3
grep "category: 'mechanic'" src/traits/traitData.ts | wc -l # expect 4
grep "category: 'rule_change'" src/traits/traitData.ts | wc -l # expect 1
```

**Check 3: Gem count and no CDR**
```bash
grep -c "^  id:" src/gems/gemData.ts    # expect 8
grep "cooldown" src/gems/gemData.ts      # expect NO matches
```

**Check 4: MatchConfig has traitId and gemAssignments**
```bash
grep "traitId" src/types.ts
grep "gemAssignments" src/types.ts
grep "traitId" src/systems/MatchOrchestrator.ts
grep "gemAssignments" src/systems/MatchOrchestrator.ts
```

**Check 5: TraitSystem event subscriptions**
```bash
grep "EventBus.on" src/traits/TraitSystem.ts
grep "EventBus.off" src/traits/TraitSystem.ts
```

**Check 6: Incompatibility blacklists**
```bash
grep "incompatiblePassives" src/traits/traitData.ts | grep -v "^\s*//"
```
Must show blacklists for vampiric_pact (bd_passive), executioner (ld_passive), spell_burn (fw_passive, vs_passive).

**Check 7: DraftScene trait + gem display**
```bash
grep "TRAIT:" src/scenes/DraftScene.ts
grep "GEM:" src/scenes/DraftScene.ts
```

**Check 8: HUD trait + gem indicators**
```bash
grep "traitText" src/ui/HUD.ts
grep "gemText" src/ui/HUD.ts
```

**Check 9: BattleScene cleanup**
```bash
grep "traitSystem.*destroy" src/scenes/BattleScene.ts
```

**Check 10: Entity type guards in TraitSystem**
```bash
grep "entityType" src/traits/TraitSystem.ts
grep "isAlive" src/traits/TraitSystem.ts
```

**Check 11: No CDR gems (explicit safety check)**
```bash
grep -i "cooldown" src/gems/gemData.ts
```
Must return no matches.

**Check 12: Stat mods applied before hero creation**
```bash
grep "applyStatMods" src/scenes/BattleScene.ts
```

Write results to `.planning/phases/05-battle-traits/05-VERIFICATION.md` with the following format:

```markdown
# Phase 5: Battle Traits & Gems — Verification Report

**Date:** [current date]
**Status:** [PASS or FAIL]

## Success Criteria

### SC-1: Battle Trait randomly selected at match start, displayed in draft scene and HUD
- [x/fail] MatchOrchestrator.generateMatch() returns traitId
- [x/fail] DraftScene renders trait banner with name + description
- [x/fail] HUD shows trait indicator below kill score
- **Status:** PASS/FAIL

### SC-2: Each hero starts with a Gem providing stat modifier, shown in UI
- [x/fail] MatchOrchestrator assigns random gems to all heroes
- [x/fail] BattleScene applies gem stat modifiers before HeroRegistry.create()
- [x/fail] DraftScene shows gem info on each hero card
- [x/fail] HUD shows player gem in stat panel
- **Status:** PASS/FAIL

### SC-3: At least 8 Battle Traits covering stat, mechanic, rule-change (rule-change capped at 1)
- [x/fail] 8 traits in traitData.ts
- [x/fail] 3 stat traits
- [x/fail] 4 mechanic traits
- [x/fail] 1 rule_change trait
- [x/fail] selectTrait selects from eligible pool (no multi-rule-change risk since only 1 exists)
- **Status:** PASS/FAIL

### SC-4: No trait+passive+gem combination breaks match
- [x/fail] Incompatibility blacklists: vampiric_pact vs bd_passive
- [x/fail] Incompatibility blacklists: executioner vs ld_passive
- [x/fail] Incompatibility blacklists: spell_burn vs fw_passive, vs_passive
- [x/fail] No CDR gems (eliminates cooldown exploit vector)
- [x/fail] All modifiers additive-then-cap (HP floor 100)
- [x/fail] Entity type guards in TraitSystem handlers
- **Status:** PASS/FAIL

## Structural Checks

- [x/fail] TypeScript compiles with zero errors
- [x/fail] TraitSystem cleanup in BattleScene.shutdown()
- [x/fail] Stat mods applied to scaledStats copy, not heroDataMap

## Files Created/Modified

| File | Status |
|------|--------|
| src/types.ts | Modified (TraitDef, GemDef, MatchConfig) |
| src/traits/traitData.ts | Created (8 traits) |
| src/traits/TraitSystem.ts | Created (runtime system) |
| src/gems/gemData.ts | Created (8 gems) |
| src/systems/EventBus.ts | Modified (TRAIT_APPLIED event) |
| src/systems/MatchOrchestrator.ts | Modified (trait + gem selection) |
| src/scenes/BattleScene.ts | Modified (trait/gem integration) |
| src/scenes/DraftScene.ts | Modified (trait/gem display) |
| src/ui/HUD.ts | Modified (trait/gem indicators) |
```

If any check fails, note the specific failure and what needs to be fixed. If all pass, status is PASS.
  </action>
  <verify>The verification report itself serves as verification. File exists at `.planning/phases/05-battle-traits/05-VERIFICATION.md` with PASS/FAIL status for all 4 success criteria.</verify>
  <done>Verification report produced. All 4 success criteria confirmed via code inspection and TypeScript compilation. Phase 5 is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and ROADMAP.md for Phase 5 completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**Only if all verification checks pass:**

1. Update `.planning/STATE.md`:
   - Change "Current Position" phase to "5 of 8 (Battle Traits & Gems) -- COMPLETE"
   - Update plan count: "Plan: 5 of 5 -- all done"
   - Update status: "Phase 5 complete -- TraitRegistry, TraitSystem, GemRegistry, DraftScene display, HUD indicators"
   - Update last activity date
   - Update progress bar and percentage to ~70%
   - Add Phase 5 to the performance table
   - Update "Current focus" to "Phase 6 -- Neutral Camps & Arena"

2. Update `.planning/ROADMAP.md`:
   - Change Phase 5 checkbox from `[ ]` to `[x]`
   - Update plans count: `**Plans:** 5 plans`
   - Add plan list with checkboxes:
     ```
     Plans:
     - [x] 05-01-PLAN.md -- TraitDef/GemDef types + traitData + gemData registries
     - [x] 05-02-PLAN.md -- TraitSystem runtime + MatchOrchestrator integration + BattleScene wiring
     - [x] 05-03-PLAN.md -- DraftScene trait banner + per-hero gem display
     - [x] 05-04-PLAN.md -- HUD trait indicator + gem indicator in stat panel
     - [x] 05-05-PLAN.md -- Verification + STATE/ROADMAP update
     ```
   - Update progress table: Phase 5 row to "5/5 | Complete | [date]"

**If any verification check fails:** Do NOT mark as complete. Instead, note the failures in STATE.md pending todos and leave Phase 5 as incomplete.
  </action>
  <verify>Check STATE.md shows Phase 5 complete. Check ROADMAP.md has Phase 5 checkbox checked and plan list populated.</verify>
  <done>STATE.md and ROADMAP.md updated to reflect Phase 5 completion. Progress at ~70%.</done>
</task>

</tasks>

<verification>
- `.planning/phases/05-battle-traits/05-VERIFICATION.md` exists with all checks documented
- All 4 success criteria from ROADMAP pass
- STATE.md reflects Phase 5 completion
- ROADMAP.md Phase 5 checkbox is checked with plan list
</verification>

<success_criteria>
1. Verification report exists with PASS for all 4 success criteria
2. TypeScript compiles with zero errors
3. All structural checks pass (cleanup, entity guards, no CDR, blacklists)
4. STATE.md and ROADMAP.md updated for Phase 5 completion
</success_criteria>

<output>
After completion, create `.planning/phases/05-battle-traits/05-05-SUMMARY.md`
</output>
